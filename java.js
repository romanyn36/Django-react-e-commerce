// a=[...Array(10).keys()]
// console.log(a)

// const carItems= [];
// const item ={product:1, qty:1}

// // check if the item is already in the cart
// const existItem = carItems.find(x => x.product === item.product)
// console.log("existItem", existItem);
const page=1
const x=1
console.log(x + 1 === page)